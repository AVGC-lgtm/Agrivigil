import 'package:flutter/material.dart';
import 'legal/fir_case_list.dart';

class LegalModule extends StatelessWidget {
  const LegalModule({super.key});

  @override
  Widget build(BuildContext context) {
    return const FIRCaseListScreen();
  }
}
