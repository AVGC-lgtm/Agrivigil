import 'package:flutter/material.dart';
import 'seizure_logging/seizure_list.dart';

class SeizureLoggingModule extends StatelessWidget {
  const SeizureLoggingModule({super.key});

  @override
  Widget build(BuildContext context) {
    return const SeizureListScreen();
  }
}
